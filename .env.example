# Hub gate: copy to `.env.local` (gitignored) and set your password.
# Use double quotes if the value contains # or spaces, e.g. VITE_HUB_PASSWORD="your#complex$secret"
# In Netlify: Site settings → Environment variables → add VITE_HUB_PASSWORD (same name).
VITE_HUB_PASSWORD=
